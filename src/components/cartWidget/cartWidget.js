import React from "react";
//import {AccessAlarm} from '@mui/icons-material';
import ShoppingCartIcon from '@mui/icons-material/ShoppingCart';

const Cart = () => {
    return (
        <div><ShoppingCartIcon sx={{color: "white"}} /> <span style={{color:"white"}} >1</span> </div>
    );
};

export default Cart;